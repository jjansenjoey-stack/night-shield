[build]
  command = "npm run build"
  publish = "dist"

# A single-page app: every path has to be answered with index.html so that
# deep links like /workshops or /route/<id> survive a hard refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
